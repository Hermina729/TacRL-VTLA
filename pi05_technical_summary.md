# π0.5 技术总结文档

## 1. 核心贡献概述

### 1.1 研究目标
π0.5是一个面向开放世界泛化的视觉-语言-动作(VLA)模型，旨在解决机器人在**完全未见过的环境**中执行复杂任务的问题。

### 1.2 核心创新点
1. **异构数据联合训练(Co-training)**：首次系统性地整合多种数据源
2. **层次化推理架构**：高层语义预测 + 低层动作执行
3. **离散+连续混合动作表示**：预训练用离散token，推理用flow matching
4. **开放世界泛化能力**：在完全未见过的真实家庭环境中执行10-15分钟的长时程任务

### 1.3 关键数据
- **移动机械臂数据(MM)**：400小时，100+家庭环境
- **多环境静态机械臂(ME)**：更广泛的场景覆盖
- **实验室跨机器人平台(CE)**：包含OXE数据集
- **高层子任务标注(HL)**：语义级任务分解
- **多模态网络数据(WD)**：图像描述、VQA、目标定位

---

## 2. 模型架构详解

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      π0.5 模型架构                           │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  输入层                                                       │
│  ├─ 4x RGB相机图像 (前/后/双腕)                              │
│  ├─ 语言指令 (tokenized)                                     │
│  └─ 本体感知状态 (关节角度, 夹爪姿态, 基座速度)              │
│                                                               │
│  ┌───────────────────────────────────────┐                  │
│  │   VLM Backbone (2.6B参数)            │                  │
│  │   ├─ SigLIP Vision Encoder (400M)    │                  │
│  │   └─ Gemma Language Model (2B)       │                  │
│  └───────────────────────────────────────┘                  │
│                    ↓                                          │
│  ┌───────────────────────────────────────┐                  │
│  │   高层推理：输出子任务                │                  │
│  │   例如: "pick up the plate"           │                  │
│  └───────────────────────────────────────┘                  │
│                    ↓                                          │
│  ┌───────────────────────────────────────┐                  │
│  │   Action Expert (300M参数)            │                  │
│  │   └─ Flow Matching预测连续动作        │                  │
│  └───────────────────────────────────────┘                  │
│                    ↓                                          │
│  输出层                                                       │
│  └─ 动作序列: 50Hz, chunk size=50                           │
│     (双臂7DoF + 双夹爪 + 基座3DoF + 升降机构)                │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 关键组件

#### 2.2.1 VLM Backbone
- **基础模型**: PaliGemma (SigLIP + Gemma)
- **参数量**: 2.6B (SigLIP 400M + Gemma 2B)
- **输入处理**:
  - 图像 → SigLIP编码 → 256 tokens/图像
  - 文本 → Tokenization → 最多20 tokens
  - 本体感知 → Tokenization → 1 token

#### 2.2.2 Action Expert
- **参数量**: 300M
- **架构**: 小型transformer
- **输入**: 带噪声的动作序列 a^{τ,ω}_{t:t+H}
- **输出**: Flow matching的向量场
- **关键创新**: 使用adaptive RMSNorm注入时间步信息

#### 2.2.3 注意力机制
```
Attention Mask Pattern:
┌──────────┬────────┬───────────┬──────────┬──────────────┐
│          │ Image  │  Prompt   │  FAST    │  Action      │
│          │        │  + State  │  Tokens  │  Expert      │
├──────────┼────────┼───────────┼──────────┼──────────────┤
│ Image    │   ✓    │     ✓     │    ✓     │      ✗       │
│ Prompt   │   ✓    │     ✓     │    ✓     │      ✗       │
│ FAST     │   ✓    │     ✓     │  Causal  │      ✗       │
│ Action   │   ✓    │     ✓     │    ✗     │  Bidir.      │
└──────────┴────────┴───────────┴──────────┴──────────────┘

✓ = 可以注意  ✗ = 不能注意  Causal = 因果注意  Bidir. = 双向注意
```

**设计原则**:
- VLM部分使用双向注意力（图像+文本）
- FAST token使用因果注意力（自回归生成）
- Action Expert独立处理，不与FAST token交互
- 信息单向流动: VLM → Action Expert

---

## 3. 训练流程

### 3.1 两阶段训练策略

```
阶段1: Pre-training (280k steps)
─────────────────────────────────
目标: 广泛的跨领域知识获取
方法: 离散token预测 (FAST tokenizer)
数据: MM + ME + CE + HL + WD (97.6%非移动机械臂数据)
优化: 仅优化文本交叉熵损失

          ↓

阶段2: Post-training (80k steps)
─────────────────────────────────
目标: 移动机械臂任务特化
方法: 离散token + 连续flow matching
数据: MM + ME + HL + WD + VI (增加语言监督)
优化: 联合损失 (α=10.0)
```

### 3.2 联合训练目标

**Pre-training目标**:
```
L_pre = H(x_{1:M}, f^ℓ_θ(o_t, ℓ))
```
- H: 交叉熵损失
- x_{1:M}: 文本token（包括FAST编码的动作）
- f^ℓ_θ: VLM输出

**Post-training目标**:
```
L_post = H(x_{1:M}, f^ℓ_θ(o_t, ℓ)) 
       + α · ||ω - a_{t:t+H} - f^a_θ(a^{τ,ω}_{t:t+H}, o_t, ℓ)||²
```
- 第一项: 保持文本生成能力（包括高层子任务预测）
- 第二项: Flow matching损失（α=10.0）
- f^a_θ: Action Expert输出

### 3.3 数据配比（Pre-training阶段）

| 数据源 | 描述 | 占比 |
|--------|------|------|
| MM | 移动机械臂，多环境 | ~2.4% |
| ME | 静态机械臂，多环境 | ~30% |
| CE | 跨平台实验室数据 | ~40% |
| HL | 高层子任务标注 | ~5% |
| WD | 网络多模态数据 | ~22% |

---

## 4. 动作表示方案

### 4.1 FAST Action Tokenizer

**原理**: 使用DCT（离散余弦变换）压缩动作序列

```
连续动作 → 归一化 → DCT变换 → 量化 → 离散token
  ↓
[a_1, a_2, ..., a_50]  (50个连续值)
  ↓
<tok_125> <tok_847> ... <tok_312>  (约8-12个token)
```

**优势**:
- 训练速度快5倍（相比纯flow matching）
- 更容易扩展到大规模数据

### 4.2 Flow Matching

**原理**: 学习从噪声到真实动作的向量场

```
时间步: τ ∈ [0,1]
噪声动作: a^{τ,ω} = τ·a + (1-τ)·ω, ω~N(0,I)
目标: 预测 ω - a

时间步采样策略: Beta分布 (α=1.5, β=1.0)
强调早期时间步 → 更好的训练稳定性
```

**推理过程** (10步去噪):
```
初始化: a^{0,ω} ~ N(0,I)
For τ in [0.1, 0.2, ..., 1.0]:
    v = f^a_θ(a^{τ,ω}, observation, subtask)
    a^{τ+δ,ω} = a^{τ,ω} + δ·v
输出: a^{1.0,ω}  (干净的动作序列)
```

### 4.3 混合策略的优势

| 方面 | FAST (离散) | Flow Matching (连续) | π0.5混合策略 |
|------|------------|---------------------|--------------|
| 训练效率 | ✓✓✓ | ✓ | ✓✓✓ (预训练) |
| 推理速度 | ✗ (自回归慢) | ✓✓✓ | ✓✓✓ (后训练) |
| 动作精度 | ✓ | ✓✓✓ | ✓✓✓ |
| 语言能力 | ✓✓✓ | ✓✓ | ✓✓✓ |

---

## 5. 层次化推理机制

### 5.1 推理流程

```
Step 1: 高层推理 (每2秒)
─────────────────────────
输入: 当前观察 + 高层任务指令
     例如: "clean the kitchen"
     
VLM输出子任务:
     "pick up the plate"
     
Step 2: 低层推理 (50Hz)
─────────────────────────
输入: 当前观察 + 子任务指令
     
Action Expert生成动作序列:
     [a_t, a_{t+1}, ..., a_{t+49}]
     
执行: 实时跟踪目标姿态
```

### 5.2 分解示例

**任务**: "put the dishes in the sink"

```
高层推理序列:
1. "pick up the plate"      → 低层执行 (30秒)
2. "put plate in the sink"  → 低层执行 (25秒)
3. "pick up the bowl"       → 低层执行 (35秒)
4. "put bowl in the sink"   → 低层执行 (28秒)
5. "pick up the cup"        → 低层执行 (32秒)
6. "put cup in the sink"    → 低层执行 (30秒)

总时长: ~3分钟
```

### 5.3 为什么需要层次化？

**实验证据**（Figure 13）:
- π0.5 (with HL): 83%成功率
- No HL (flat policy): 39%成功率
- Implicit HL (训练时有HL数据，推理时不用): 73%成功率

**关键发现**:
1. 高层推理显著提升长时程任务性能
2. 即使不显式预测子任务，HL数据在训练时也有帮助
3. GPT-4作为高层策略表现差（38%），说明需要机器人域的微调

---

## 6. 数据流图

### 6.1 Pre-training数据流

```
                    ┌──────────────────┐
                    │  数据源混合      │
                    └────────┬─────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
   ┌────▼────┐         ┌────▼────┐         ┌────▼────┐
   │ Robot   │         │  High   │         │   Web   │
   │ Actions │         │  Level  │         │  Data   │
   │ (FAST)  │         │ Subtask │         │  (VQA)  │
   └────┬────┘         └────┬────┘         └────┬────┘
        │                   │                    │
        └───────────┬───────┴──────┬────────────┘
                    │              │
              ┌─────▼──────┐ ┌────▼─────────┐
              │ Tokenize   │ │ Tokenize     │
              │ Actions    │ │ Text/Bbox    │
              └─────┬──────┘ └────┬─────────┘
                    │              │
                    └──────┬───────┘
                           │
                    ┌──────▼──────┐
                    │ VLM         │
                    │ Transformer │
                    │ (Auto-reg.) │
                    └─────────────┘
                           │
                    Next Token Prediction
```

### 6.2 Post-training数据流

```
                    ┌──────────────────┐
                    │ 数据源 (筛选后)  │
                    │ MM+ME+HL+WD+VI   │
                    └────────┬─────────┘
                             │
                    ┌────────┴─────────┐
                    │                  │
            ┌───────▼─────┐    ┌──────▼──────┐
            │ FAST Token  │    │  Flow       │
            │ (High-Level)│    │  Matching   │
            │             │    │  (Actions)  │
            └───────┬─────┘    └──────┬──────┘
                    │                 │
                    │        ┌────────▼────────┐
                    │        │  Add Noise      │
                    │        │  a^{τ,ω}        │
                    │        └────────┬────────┘
                    │                 │
            ┌───────▼─────┐    ┌──────▼──────┐
            │    VLM      │    │   Action    │
            │ Transformer │    │   Expert    │
            └───────┬─────┘    └──────┬──────┘
                    │                 │
                    │                 │
            ┌───────▼─────┐    ┌──────▼──────┐
            │ Cross-Ent.  │    │ Flow Match  │
            │ Loss        │    │ Loss (α=10) │
            └───────┬─────┘    └──────┬──────┘
                    │                 │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │ Joint Objective │
                    └─────────────────┘
```

### 6.3 Inference数据流

```
观察输入 (4x Images + Robot State)
            │
            ▼
    ┌───────────────┐
    │ High-Level    │ ← "clean the kitchen"
    │ Inference     │
    │ (VLM)         │
    └───────┬───────┘
            │
            ▼
    "pick up the plate"  (子任务)
            │
            ▼
    ┌───────────────┐
    │ Low-Level     │ ← 观察 + 子任务
    │ Inference     │
    │ (Action Exp.) │
    └───────┬───────┘
            │
            ▼
    初始噪声 a^{0,ω}
            │
    ┌───────▼───────┐
    │ Flow Matching │ (10 steps)
    │ Denoising     │
    └───────┬───────┘
            │
            ▼
    干净动作序列 a_{t:t+50}
            │
            ▼
    PD控制器 → 机器人执行
```

---

## 7. 关键技术细节

### 7.1 归一化策略
```python
# 动作归一化到[-1, 1]
for each dimension d:
    lower_bound = 1st_percentile(actions[:, d])
    upper_bound = 99th_percentile(actions[:, d])
    normalized_actions[:, d] = 2 * (actions[:, d] - lower_bound) / 
                                (upper_bound - lower_bound) - 1
```

### 7.2 图像增强
```python
transforms = [
    RandomCrop(0.95 * size),
    Resize(original_size),
    Rotate(-5° to 5°),
    ColorJitter(brightness=0.3, contrast=0.4, saturation=0.5)
]
```

### 7.3 控制频率
- **高层推理**: ~0.5 Hz（约每2秒一次）
- **低层推理**: 50 Hz（每20ms）
- **Action Chunk**: 50步 = 1秒的动作序列

### 7.4 硬件需求

**训练**:
- GPU内存: >22.5GB (LoRA微调), >40GB (全量微调)
- 推荐: A100 40GB或H100

**推理**:
- GPU内存: >8GB
- 单个H100: 63ms生成一个action chunk (5相机+10去噪步)

---

## 8. 实验结果摘要

### 8.1 主要发现

| 实验 | 关键结论 | 数据支持 |
|------|----------|----------|
| 真实家庭评估 | π0.5可在完全未见环境中成功执行复杂任务 | 3个真实家庭，平均75%任务完成度 |
| 环境数量scaling | 训练环境越多，泛化性能越好 | 3→104环境，性能从15%→82% |
| 数据源消融 | 所有数据源都重要，ME和CE最关键 | 移除ME或CE导致性能下降20-30% |
| 与π0对比 | π0.5显著优于π0 | π0.5: 82% vs π0: 20% |
| 层次推理 | 高层推理对长时程任务至关重要 | With HL: 83% vs No HL: 39% |

### 8.2 语言跟随能力

| 对象类型 | 跟随率 | 成功率 |
|----------|--------|--------|
| 分布内物体 | 88% | 72% |
| 分布外物体 | 68% | 42% |

**关键**: Web data (WD) 对分布外物体的语言跟随至关重要

### 8.3 计算效率对比

| 模型 | 训练时间 (相对) | 推理延迟 |
|------|----------------|----------|
| π0 (纯flow) | 5x | 63ms |
| π0-FAST (纯自回归) | 1x | >300ms |
| π0.5 (混合) | 1x | 63ms |

---

## 9. 局限性与未来方向

### 9.1 当前局限
1. **错误类型**:
   - 部分可观察性问题（手臂遮挡）
   - 高层策略有时重复子任务
   - 对特殊物体（如难开的抽屉）泛化不足

2. **指令复杂度**:
   - 目前仅处理简单指令
   - 需要更复杂的标注或合成数据

3. **上下文长度**:
   - 无跨房间记忆
   - 不记得物体存放位置

### 9.2 未来方向
1. **更丰富的监督**:
   - 人类语言演示(VI)显示潜力
   - 探索更多人机交互方式

2. **更广泛的数据源**:
   - 人类视频
   - 模拟数据
   - 更多机器人平台

3. **更长的上下文**:
   - 跨步骤记忆
   - 场景理解与建图

---

## 10. 与其他VLA的对比

| 模型 | 发布时间 | 核心特点 | 评估环境 | 性能水平 |
|------|----------|----------|----------|----------|
| RT-1 | 2022 | 首个大规模VLA | 训练环境内 | Baseline |
| RT-2 | 2023 | Web知识迁移 | 训练环境内 | 更好语言理解 |
| π0 | 2024 | Flow matching | 训练环境内 | 更精细控制 |
| OpenVLA | 2024 | 开源基线 | 训练环境内 | 社区标准 |
| **π0.5** | **2025** | **开放世界泛化** | **完全未见环境** | **突破性泛化** |

**π0.5的独特价值**:
- 首次在完全未见的真实家庭中成功执行长时程任务
- 系统性的异构数据联合训练框架
- 10-15分钟的复杂多步骤行为

---

## 11. 关键参数速查表

### 训练超参数
```
Pre-training:
- Steps: 280k
- Learning rate: 默认PaliGemma学习率
- Batch size: 根据GPU内存调整
- Optimizer: AdamW
- Loss weight α: 0 (仅文本loss)

Post-training:
- Steps: 80k
- Learning rate: 同上
- Loss weight α: 10.0
- Flow matching: 10 denoising steps
```

### 模型参数
```
VLM Backbone:
- Width: 2048
- Depth: 18
- MLP dim: 16384
- Num heads: 18
- Total params: 2.6B

Action Expert:
- Width: 1024
- Depth: 18
- MLP dim: 4096
- Total params: 300M
```

### 数据参数
```
Action space: 18-19 DoF
- 2x 7-DoF arms
- 2x 1-DoF grippers  
- 3-DoF base velocity
- 1-2 DoF lift

Image input: 4 cameras
- Front, rear, left wrist, right wrist
- Resolution: (具体未在论文中说明)

Action chunk: 50 steps @ 50Hz = 1 second
```

---

## 12. 总结

π0.5代表了机器人基础模型向**真实世界泛化**迈出的关键一步。通过：

1. **系统性的数据整合** - 异构数据联合训练
2. **高效的架构设计** - 层次推理 + 混合动作表示  
3. **科学的训练策略** - 两阶段训练流程

π0.5实现了在完全未见环境中执行复杂长时程任务的能力，为通用机器人智能奠定了基础。

**关键启示**:
- **数据多样性 > 数据量**: 400小时的移动机械臂数据 + 异构数据 > 单一来源的大量数据
- **层次化是必需的**: 长时程任务需要高层推理
- **预训练很重要**: 离散token预训练提供了强大的初始化
- **混合方法最优**: 结合不同方法的优点
